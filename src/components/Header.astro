<header>
    <div>
        <img class="logo" src="assets/paw.svg" alt="" />

        <form>
            <label for="search">Pesquise por artigos</label>
            <input id="search" type="text" placeholder="Pesquise por artigos" />
        </form>

        <img class="avatar" src="public/assets/photos/woman.png" alt="" />
    </div>
</header>

<style>
    header {
        background-color: var(--color-base-sky-700);
        padding: 1.2rem 2.8rem;
    }

    header div {
        max-width: 1100px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 2rem;
    }

    .logo {
        width: 4rem;
    }

    form {
        flex: 1;
    }

    label {
        width: 1px;
        height: 1px;
        overflow: hidden;
        position: absolute;
    }

    input {
        width: 100%;
        padding: 1.2rem 1.4rem;
        border-radius: 4px;
        background-color: var(--color-base-sky-600);
        border: none;
        color: var(--color-base-white);
        font-weight: 600;
    }

    input::placeholder {
        color: var(--color-base-white);
        font-weight: 600;
    }

    .avatar {
        width: 4rem;
        height: 4rem;
        border-radius: 999px;
        object-fit: cover;
    }
</style>
